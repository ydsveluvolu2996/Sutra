"use client";import{useEffect,useState}from"react";import type{AwsHealthDashboardFilters}from"../../lib/finops-aws-health-dashboard";import styles from"./finops-health-events-dashboard.module.css";type Report=ReturnType<typeof import("../../lib/finops-aws-health-dashboard").buildAwsHealthPlanningDashboard>&{connectionId:string;sourceState:string;availability:{configurationState:string;collectionState:string;supportPlan:string;eligibleSupport:boolean;organizationsAllFeaturesEnabled:boolean;organizationViewStatus:string;collectorAccountType:string;delegatedAdministratorRegistered:boolean;initialLoadState:string;observedAt:string}|null;evidence:Readonly<Record<string,unknown>>;collection:{available:false;reason:string}};const EMPTY:AwsHealthDashboardFilters={status:null,category:null,service:null,accountId:null,region:null,actionability:null,search:null};function Select({label,value,options,onChange}:{label:string;value:string|null;options:readonly string[];onChange:(value:string|null)=>void}){return<label>{label}<select value={value??""} onChange={(e)=>onChange(e.target.value||null)}><option value="">All</option>{options.map((option)=><option key={option}>{option.replaceAll("_"," ")}</option>)}</select></label>}function cell(value:string){const safe=/^[=+\-@\t\r]/u.test(value)?`'${value}`:value;return`"${safe.replaceAll('"','""')}"`}function exportCsv(report:Report){const rows=report.events.flatMap(({event})=>event.affectedEntities.length?event.affectedEntities.map((entity)=>[event.status,event.category,event.service??"",event.region??"",event.eventTypeCode,event.startAt??"",event.endAt??"",event.actionability??"",entity.accountId??"",entity.entityValue??"",entity.status??"",event.details.find((detail)=>detail.accountId===entity.accountId)?.description??""].map((value)=>cell(value)).join(",")):[[event.status,event.category,event.service??"",event.region??"",event.eventTypeCode,event.startAt??"",event.endAt??"",event.actionability??"","","","",event.details[0]?.description??""].map((value)=>cell(value)).join(",")]);const header="status,category,service,region,event_type,start,end,actionability,account,affected_entity,entity_status,description";const url=URL.createObjectURL(new Blob([[header,...rows].join("\n")],{type:"text/csv;charset=utf-8"}));const a=document.createElement("a");a.href=url;a.download="sutra-health-events-planning.csv";a.click();URL.revokeObjectURL(url)}
export function HealthEventsReportView({report,filters,onFiltersChange}:{report:Report;filters:AwsHealthDashboardFilters;onFiltersChange:(filters:AwsHealthDashboardFilters)=>void}){const set=<K extends keyof AwsHealthDashboardFilters>(key:K,value:AwsHealthDashboardFilters[K])=>onFiltersChange({...filters,[key]:value});return<section className={styles.root} aria-label="AWS Health Events planning dashboard"><div role="status" className={styles.lag}><strong>Planning view — not real time.</strong> AWS Health organization data can lag by 48 hours or more. Use AWS Health and your incident tooling for current response.</div><div className={styles.privacy}><strong>Tenant-private evidence.</strong> Event descriptions, account IDs, affected entities and metadata are visible only to authenticated users authorized for this connection.</div><section className={styles.prereq}><article><span>Eligible support / API entitlement</span><strong>{report.availability?.eligibleSupport?"Validated":"Not validated"}</strong><small>{report.availability?.supportPlan??"Unknown plan"}</small></article><article><span>AWS Organizations access</span><strong>{report.availability?.organizationsAllFeaturesEnabled?"All features enabled":"Not proven"}</strong><small>{report.availability?.collectorAccountType??"Unknown collector"}</small></article><article><span>Organizational View provider state</span><strong>{report.availability?.organizationViewStatus??"UNKNOWN"}</strong><small>{report.availability?.initialLoadState??"UNKNOWN"} initial load</small></article></section><div className={styles.filters}><Select label="Past / current / upcoming" value={filters.status} options={report.filterOptions.statuses} onChange={(v)=>set("status",v as AwsHealthDashboardFilters["status"])}/><Select label="Category" value={filters.category} options={report.filterOptions.categories} onChange={(v)=>set("category",v as AwsHealthDashboardFilters["category"])}/><Select label="Service" value={filters.service} options={report.filterOptions.services} onChange={(v)=>set("service",v)}/><Select label="Affected account" value={filters.accountId} options={report.filterOptions.accounts} onChange={(v)=>set("accountId",v)}/><Select label="Region" value={filters.region} options={report.filterOptions.regions} onChange={(v)=>set("region",v)}/><Select label="Actionability" value={filters.actionability} options={report.filterOptions.actionabilities} onChange={(v)=>set("actionability",v as AwsHealthDashboardFilters["actionability"])}/><label>Search<input value={filters.search??""} maxLength={128} onChange={(e)=>set("search",e.target.value||null)}/></label></div><section className={styles.section}><header><h3>Organization Health planning summary</h3><button onClick={()=>exportCsv(report)} type="button">Export visible evidence</button></header><div className={styles.cards}><article><span>Past / closed</span><strong>{report.summary.pastCount}</strong></article><article><span>Current / open</span><strong>{report.summary.currentCount}</strong></article><article><span>Upcoming</span><strong>{report.summary.upcomingCount}</strong></article><article><span>Action required</span><strong>{report.summary.actionRequiredCount}</strong></article><article><span>Affected accounts</span><strong>{report.summary.affectedAccountCount}</strong></article><article><span>Affected entities</span><strong>{report.summary.affectedEntityCount}</strong></article></div></section><section className={styles.section}><header><h3>Events, affected entities &amp; details</h3><span>{report.eventsTruncated?"First 500 rows":"Complete selected view"}</span></header><div className={styles.events}>{report.events.map(({event,generationId,observedAt})=><details key={event.arn}><summary><span className={`${styles.badge} ${styles[event.status]}`}>{event.status}</span><strong>{event.eventTypeCode}</strong><span>{event.service??"Unknown service"} · {event.region??"Global"}</span><time>{event.startAt??"No start supplied"}</time></summary><div className={styles.eventBody}><p><strong>{event.actionability??"Actionability unavailable"}</strong> · {event.category} · {event.scope}</p><p>{event.details.map((detail)=>detail.description).filter(Boolean).join(" ")||"No accepted event description."}</p><h4>Affected accounts and entities</h4>{event.affectedEntities.length?<div className={styles.scroll}><table><thead><tr><th>Account</th><th>Entity</th><th>Status</th><th>Last updated</th><th>Metadata</th></tr></thead><tbody>{event.affectedEntities.map((entity,index)=><tr key={`${entity.accountId}:${entity.entityArn}:${entity.entityValue}:${index}`}><td>{entity.accountId??"Public event"}</td><td>{entity.entityValue??entity.entityArn??"Not supplied"}</td><td>{entity.status??"Unknown"}</td><td>{entity.lastUpdatedAt??"Unknown"}</td><td>{entity.metadata.map((item)=>`${item.key}=${item.value}`).join(" · ")}</td></tr>)}</tbody></table></div>:<p>No affected entity was returned for this event.</p>}<small>Accepted {observedAt} · generation {generationId} · provider failures {event.evidence.providerFailures.join(", ")||"none"}</small></div></details>)}</div></section><section className={styles.section}><header><h3>Immutable event history</h3><span>{report.summary.historyGenerationCount} accepted snapshots</span></header>{report.eventHistory.map((item)=><article className={styles.timeline} key={item.arn}><strong>{item.eventTypeCode}</strong><div>{item.points.map((point)=><span key={`${point.observedAt}:${point.status}`}>{point.observedAt.slice(0,10)} · {point.status}</span>)}</div></article>)}</section><details className={`${styles.section} ${styles.evidence}`}><summary>Capture hashes, coverage and limitations</summary><pre>{JSON.stringify({planningSemantics:report.planningSemantics,freshness:report.freshness,lineage:report.lineage,evidence:report.evidence,collection:report.collection,limitations:report.limitations},null,2)}</pre></details></section>}
export function FinopsHealthEventsDashboard({connectionId}:{connectionId:string|null}){const[filters,setFilters]=useState(EMPTY);const[state,setState]=useState<{report:Report|null;error:string|null;availability:Report["availability"]}>({report:null,error:null,availability:null});useEffect(()=>{if(connectionId===null)return;const controller=new AbortController();const p=new URLSearchParams({connectionId});for(const[key,value]of Object.entries(filters))if(value!==null)p.set(key,value);fetch(`/api/v1/finops/health-events?${p}`,{signal:controller.signal}).then(async(r)=>{if(!r.ok)throw new Error("Health Events request failed");return r.json()}).then((value)=>value.dashboard===null?setState({report:null,error:null,availability:value.availability}):setState({report:value as Report,error:null,availability:value.availability})).catch((error)=>{if(!controller.signal.aborted)setState({report:null,error:error instanceof Error?error.message:"Request failed",availability:null})});return()=>controller.abort()},[connectionId,filters]);if(connectionId===null)return<div role="status" className={styles.warning}>Connect an active AWS trust-role account.</div>;if(state.error)return<div role="alert" className={styles.error}>{state.error}</div>;if(!state.report){const reason=state.availability?.eligibleSupport===false?"Eligible AWS support/API entitlement is not validated.":state.availability?.organizationViewStatus==="DISABLED"?"AWS Health Organizational View is disabled.":state.availability?.organizationsAllFeaturesEnabled===false?"AWS Organizations all-features access is not enabled.":"No complete organization Health snapshot has been accepted.";return<div role="status" className={styles.warning}><strong>Planning view unavailable.</strong> {reason} Data can lag by 48 hours or more and discovery is not real-time.</div>}return<HealthEventsReportView report={state.report} filters={filters} onFiltersChange={setFilters}/>}
