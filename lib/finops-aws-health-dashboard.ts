import type{AwsHealthActionability,AwsHealthEventCategory,AwsHealthEventStatus,AwsHealthNormalizedEvent,AwsHealthOrganizationSnapshot}from"./finops-aws-health-organization.ts";
export interface AwsHealthAcceptedHead{readonly generationId:string;readonly contentSha256:string;readonly snapshot:AwsHealthOrganizationSnapshot}export interface AwsHealthDashboardFilters{readonly status:AwsHealthEventStatus|null;readonly category:AwsHealthEventCategory|null;readonly service:string|null;readonly accountId:string|null;readonly region:string|null;readonly actionability:AwsHealthActionability|null;readonly search:string|null}
function eventTime(event:AwsHealthNormalizedEvent,observed:string){return event.lastUpdatedAt??event.startAt??observed}
export function buildAwsHealthPlanningDashboard(heads:readonly AwsHealthAcceptedHead[],filters:AwsHealthDashboardFilters,nowMs=Date.now()){if(!Number.isSafeInteger(nowMs)||nowMs<0||heads.length>730)throw new Error("aws-health-dashboard-invalid");const history=heads.flatMap((head)=>head.snapshot.events.map((event)=>({generationId:head.generationId,contentSha256:head.contentSha256,observedAt:head.snapshot.observedAtIso,captureId:head.snapshot.captureId,event})));const current=new Map<string,typeof history[number]>();for(const item of history){const previous=current.get(item.event.arn);if(previous===undefined||eventTime(item.event,item.observedAt)>eventTime(previous.event,previous.observedAt))current.set(item.event.arn,item)}const search=filters.search?.toLocaleLowerCase()??null;const rows=[...current.values()].filter(({event})=>(filters.status===null||event.status===filters.status)&&(filters.category===null||event.category===filters.category)&&(filters.service===null||event.service===filters.service)&&(filters.accountId===null||event.affectedAccounts.includes(filters.accountId))&&(filters.region===null||event.region===filters.region)&&(filters.actionability===null||event.actionability===filters.actionability)&&(search===null||`${event.eventTypeCode} ${event.service??""} ${event.region??""} ${event.details.map((detail)=>detail.description??"").join(" ")} ${event.affectedEntities.map((entity)=>entity.entityValue??"").join(" ")}`.toLocaleLowerCase().includes(search))).sort((a,b)=>eventTime(b.event,b.observedAt).localeCompare(eventTime(a.event,a.observedAt))||a.event.arn.localeCompare(b.event.arn));const transitions=new Map<string,{arn:string;eventTypeCode:string;points:{observedAt:string;status:AwsHealthEventStatus;lastUpdatedAt:string|null;generationId:string}[]}>();for(const item of history.sort((a,b)=>a.observedAt.localeCompare(b.observedAt))){const transition=transitions.get(item.event.arn)??{arn:item.event.arn,eventTypeCode:item.event.eventTypeCode,points:[]};const previous=transition.points.at(-1);if(previous===undefined||previous.status!==item.event.status||previous.lastUpdatedAt!==item.event.lastUpdatedAt)transition.points.push({observedAt:item.observedAt,status:item.event.status,lastUpdatedAt:item.event.lastUpdatedAt,generationId:item.generationId});transitions.set(item.event.arn,transition)}const latest=heads.map((head)=>head.snapshot.observedAtIso).sort().at(-1)??null;const ageHours=latest===null?null:Math.round(Math.max(0,(nowMs-Date.parse(latest))/3_600_000)*100)/100;return{schemaVersion:"sutra.aws-health-planning-dashboard.v1",generatedAtIso:new Date(nowMs).toISOString(),filters,planningSemantics:{notRealTime:true,minimumDocumentedLagHours:48,purpose:"PLANNING_AND_HISTORICAL_ANALYSIS",warning:"AWS Health organization data can lag by 48 hours or more. Do not use this dashboard for real-time incident response."},freshness:{latestAcceptedObservedAt:latest,ageHours,staleAfterHours:72},prerequisites:heads.at(-1)?.snapshot.prerequisites??null,configurationState:heads.at(-1)?.snapshot.configurationState??"unavailable",summary:{eventCount:rows.length,pastCount:rows.filter((row)=>row.event.status==="closed").length,currentCount:rows.filter((row)=>row.event.status==="open").length,upcomingCount:rows.filter((row)=>row.event.status==="upcoming").length,actionRequiredCount:rows.filter((row)=>row.event.actionability==="ACTION_REQUIRED").length,affectedAccountCount:new Set(rows.flatMap((row)=>row.event.affectedAccounts)).size,affectedEntityCount:rows.reduce((sum,row)=>sum+row.event.affectedEntities.length,0),historyGenerationCount:heads.length},events:rows.slice(0,500),eventsTruncated:rows.length>500,eventHistory:[...transitions.values()].sort((a,b)=>a.eventTypeCode.localeCompare(b.eventTypeCode)),filterOptions:{statuses:[...new Set([...current.values()].map((row)=>row.event.status))].sort(),categories:[...new Set([...current.values()].map((row)=>row.event.category))].sort(),services:[...new Set([...current.values()].map((row)=>row.event.service).filter((item):item is string=>item!==null))].sort(),accounts:[...new Set([...current.values()].flatMap((row)=>row.event.affectedAccounts))].sort(),regions:[...new Set([...current.values()].map((row)=>row.event.region).filter((item):item is string=>item!==null))].sort(),actionabilities:[...new Set([...current.values()].map((row)=>row.event.actionability).filter((item):item is AwsHealthActionability=>item!==null))].sort()},lineage:heads.map((head)=>({generationId:head.generationId,contentSha256:head.contentSha256,captureId:head.snapshot.captureId,observedAt:head.snapshot.observedAtIso,coverage:head.snapshot.coverage})),limitations:["This is planning and historical evidence with documented 48-hour-or-greater lag, never a real-time incident feed.","AWS provider retention is limited; Sutra history contains only snapshots collected and accepted after activation.","Descriptions, affected entities and metadata are tenant-private and exposed only through authenticated same-tenant access.","An empty result does not prove that no incident exists or that provider publication has completed."]}}
